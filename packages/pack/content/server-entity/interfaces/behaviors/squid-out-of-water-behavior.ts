import { BehaviorPriority } from './behavior-priority';

/**
 * Allows the squid to stick to the ground when outside water. Can only be used by the Squid.
 */
export interface SquidOutOfWaterBehavior extends BehaviorPriority {}
