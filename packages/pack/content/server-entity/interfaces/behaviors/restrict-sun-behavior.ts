import { BehaviorPriority } from './behavior-priority';

/**
 * Allows the mob to automatically start avoiding the sun when its a clear day out.
 */
export interface RestrictSunBehavior extends BehaviorPriority {}
