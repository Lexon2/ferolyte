import { BehaviorPriority } from './behavior-priority';

/**
 * Allows the squid to swim in place idly. Can only be used by the Squid.
 */
export interface SquidIdleBehavior extends BehaviorPriority {}
