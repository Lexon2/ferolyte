import { BehaviorPriority } from './behavior-priority';

/**
 * Allows the wither to launch random attacks. Can only be used by the Wither Boss.
 */
export interface WitherRandomAttackPosGoalBehavior extends BehaviorPriority {}
