import { BehaviorPriority } from './behavior-priority';

/**
 * Allows the enderman to take a block and carry it around. Can only be used by Endermen.
 */
export interface EndermanTakeBlockBehavior extends BehaviorPriority {
  // No additional properties needed as per the JSON schema
}
