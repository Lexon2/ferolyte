import { BaseNavigation } from './base-navigation';

/**
 * Interface for the navigation_fly component
 * Defines the speed with which an entity can fly
 */
export interface NavigationFlyComponent extends BaseNavigation {}
