import { BaseNavigation } from './base-navigation';

/**
 * Interface for the navigation_swim component
 * Defines the speed with which an entity can swim
 */
export interface NavigationSwimComponent extends BaseNavigation {}
