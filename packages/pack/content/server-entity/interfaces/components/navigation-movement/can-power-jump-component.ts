import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the can_power_jump component
 * Allows an entity to perform a "power jump" (like a horse)
 */
export interface CanPowerJumpComponent extends StateObject {}
