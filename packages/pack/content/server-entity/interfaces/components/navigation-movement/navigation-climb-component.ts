import { BaseNavigation } from './base-navigation';

/**
 * Interface for the navigation_climb component
 * Defines the speed with which an entity can climb
 */
export interface NavigationClimbComponent extends BaseNavigation {}
