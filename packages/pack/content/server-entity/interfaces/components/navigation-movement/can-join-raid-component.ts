import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the can-join-raid component
 * Sets that this entity can join a raid.
 */
export interface CanJoinRaidComponent extends StateObject {}
