
/**
 * Interface for the RailMovementComponent
 * Defines the entity's movement on the rails.
 */
export interface RailMovementComponent {
  /**
   * The maximum speed the entity can move on the rail
   * @default 0.4
   */
  maxSpeed?: number;
}

