import { BaseNavigation } from './base-navigation';

/**
 * Interface for the navigation_float component
 * Defines the speed with which an entity can float
 */
export interface NavigationFloatComponent extends BaseNavigation {}
