import { MaxTurn } from "./max-turn";

/**
 * Interface for the movement_skip component
 * This move control causes the mob to hop as it moves.
 */
export interface MovementSkipComponent extends MaxTurn {
  // Inherits max_turn from MaxTurn
}
