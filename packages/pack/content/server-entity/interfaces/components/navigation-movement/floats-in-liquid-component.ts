import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the floats_in_liquid component
 * Allows an entity to float in liquid instead of sinking
 */
export interface FloatsInLiquidComponent extends StateObject {}
