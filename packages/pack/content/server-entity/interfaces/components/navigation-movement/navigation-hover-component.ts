import { BaseNavigation } from './base-navigation';

/**
 * Interface for the navigation_hover component
 * Defines the speed with which an entity can hover
 */
export interface NavigationHoverComponent extends BaseNavigation {}
