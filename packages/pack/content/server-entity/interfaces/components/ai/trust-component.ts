import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the trust component
 * Allows this entity to trust multiple players
 */
export interface TrustComponent extends StateObject {}
