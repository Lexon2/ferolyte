import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the out_of_control component
 * Indicates that an entity is out of control (e.g., for boats)
 */
export interface OutOfControlComponent extends StateObject {}
