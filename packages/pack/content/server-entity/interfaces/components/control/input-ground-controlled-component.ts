import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the input_ground_controlled component
 * When configured as a rideable entity, the entity will be controlled using WASD controls.
 */
export interface InputGroundControlledComponent extends StateObject {
}
