import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the rotation_locked_to_vehicle component
 * Aligns both the entity's body rotation and its overall rotation with that of its mounted vehicle.
 */
export interface RotationLockedToVehicleComponent extends StateObject {}
