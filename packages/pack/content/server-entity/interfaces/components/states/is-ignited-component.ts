import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the is_ignited component
 * Sets that this entity is currently on fire
 */
export interface IsIgnitedComponent extends StateObject {}
