import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the is_stackable component
 * Sets that this entity can be stacked
 */
export interface IsStackableComponent extends StateObject {}
