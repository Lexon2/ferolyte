import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the is_sheared component
 * Sets that this entity is currently sheared
 */
export interface IsShearedComponent extends StateObject {}
