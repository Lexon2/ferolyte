import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the is_hidden_when_invisible component
 * Sets that this entity can hide from hostile mobs while invisible
 */
export interface IsHiddenWhenInvisibleComponent extends StateObject {}
