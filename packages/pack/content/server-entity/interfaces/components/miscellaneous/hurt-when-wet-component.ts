import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the hurt_when_wet component
 * Specifies if an actor is hurt when wet.
 */
export interface HurtWhenWetComponent extends StateObject {
}
