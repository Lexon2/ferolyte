import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the hide component
 * Defines whether the entity should be hidden.
 */
export interface HideComponent extends StateObject {
}
