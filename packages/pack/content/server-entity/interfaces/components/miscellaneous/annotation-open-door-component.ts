import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the annotation.open_door component
 * Allows the actor to open doors assuming that that flags set up for the component to use in navigation.
 */
export interface AnnotationOpenDoorComponent extends StateObject {}
