/**
 * Interface for the default_look_angle component
 * Sets this entity's default head rotation angle.
 */
export interface DefaultLookAngleComponent {
  /**
   * Angle in degrees
   * @default 0.0
   */
  value?: number;
}
