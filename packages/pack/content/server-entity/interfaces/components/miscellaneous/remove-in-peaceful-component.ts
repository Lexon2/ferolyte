import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the remove_in_peaceful component
 * Denotes entities that are not allowed to exist in Peaceful difficulty.
 */
export interface RemoveInPeacefulComponent extends StateObject {}
