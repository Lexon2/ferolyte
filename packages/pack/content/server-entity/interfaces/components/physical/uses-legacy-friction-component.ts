import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the uses_legacy_friction component
 * When set, legacy calculations are used when applying friction_modifier.
 */
export interface UsesLegacyFrictionComponent extends StateObject {}
