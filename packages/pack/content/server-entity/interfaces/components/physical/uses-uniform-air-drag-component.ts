import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the uses_uniform_air_drag component
 * When present, air drag is applied uniformly on both vertical and horizontal axes.
 */
export interface UsesUniformAirDragComponent extends StateObject {}
