import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the body_rotation_blocked component
 * When set, the entity will no longer visually rotate their body to match their facing direction.
 */
export interface BodyRotationBlockedComponent extends StateObject {}
