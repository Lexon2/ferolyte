import { StateObject } from '../../../../common/interfaces/state-object';

/**
 * Interface for the pushable_by_block component
 * Allows the entity to be pushed by certain blocks, like Shulker Boxes and Pistons.
 */
export interface PushableByBlockComponent extends StateObject {}
