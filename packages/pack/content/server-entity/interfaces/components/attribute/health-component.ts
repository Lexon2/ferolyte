import { AttributeComponent } from '.';

/**
 * Interface for the health component
 * Defines the health and related properties of an entity
 */
export interface HealthComponent extends AttributeComponent {}
