import { EntityEventTrigger } from '../../trigger';

/**
 * Interface for the on_target_acquired component
 * Adds a trigger to call when this entity finds a target.
 */
export interface OnTargetAcquiredComponent extends EntityEventTrigger {}
