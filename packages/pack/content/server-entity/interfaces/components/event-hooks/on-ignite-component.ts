import { EntityEventTrigger } from '../../trigger';

/**
 * Interface for the on_ignite component
 * Adds a trigger to call when this entity is set on fire.
 */
export interface OnIgniteComponent extends EntityEventTrigger {}
