---
'@ferolyte/common': patch
'@ferolyte/pack': patch
'@ferolyte/cli': patch
---

Allow custom item tags in `createTags` instead of rejecting tags outside the vanilla allowlist. Only non-empty string validation remains.
